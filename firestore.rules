rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lion Park begins deny-by-default. Role-aware staff and public-read rules
    // will be added together with the authenticated operational data model.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
